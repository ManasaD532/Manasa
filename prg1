#include <stdio.h>
#include <stdio.h>
void main()
{
    int n;
    printf("enter any number");
    scanf("%d,&n");
    int count=0;
    for(int i=2;i<n;i++)
    {
        if(n%i==0)
        {
            count++;
        }
    }
    if(count==0)
    {
       printf("it is a prime");
    }
    else
        {
          printf("it is not a prime");
        }
    }
